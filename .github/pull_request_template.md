## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] No real PHI or real patient data was added anywhere (synthetic data only)
- [ ] No secrets or `.env` files were committed
- [ ] `python manage.py test tests` passes
- [ ] `npm run build` and `npm run lint` pass in `frontend/`
- [ ] SQL changes were run through `build_marts` / `run_quality_checks` locally
- [ ] README updated if setup steps or architecture changed

## Test plan

<!-- How did you verify this change? -->
